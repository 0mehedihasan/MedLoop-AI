"""Runtime facts about the machine: device resolution and seeding.

Modules: ``device`` (``DeviceKind`` -> ``ResolvedDevice``), ``seeding`` (``seed_everything``).
Import them directly; this package init stays import-free (see ``ml/__init__.py``).
"""
