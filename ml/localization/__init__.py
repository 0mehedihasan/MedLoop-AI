"""Normalised geometry and the saliency-to-region derivation.

Modules: ``geometry`` (the §4.3 dataclasses, IoU, connected components — implemented),
``from_saliency`` (threshold -> largest region -> coarse box — implemented).
Import them directly; this package init stays import-free (see ``ml/__init__.py``).
"""
